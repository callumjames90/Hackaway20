function RadarAPI(api_key, radius) {
    this.api_key = api_key;
    this.radius = radius;
    this.api_root_endpoint = "https://api.radar.io/v1/"

    function createReviewGeofence(longitude, latitude, username) {
    }

    function getTouchingGeofences(longitude, latitude) {
    }

    function createGeofence(longitude, latitude) {
    }
}
